# Optional public canonical origin. Set after your production domain is verified.
NEXT_PUBLIC_SITE_URL=https://cybermindspace-labs.pradeepmajji853.chatgpt.site
# Vercel only: optional D1 REST credentials for durable interest registration.
# Sites uses its managed DB binding; these are not needed there.
# Use an existing authorized D1 database with the interests migration applied.
# Keep the API token server-only and scoped to the necessary D1 resource.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_D1_DATABASE_ID=
CLOUDFLARE_D1_API_TOKEN=
